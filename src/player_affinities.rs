#[derive(Debug, Clone)]
pub struct PlayerAffinities {
    pub finishing: u8,
    pub shooting: u8,
    pub strength: u8,
    pub speed: u8,
    pub jumping: u8,
    pub instincts: u8,
    pub fluidity: u8,
}
