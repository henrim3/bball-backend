#[derive(Debug, Clone)]
pub struct PlayerAttributes {
    pub finishing_touch: u8,
    pub shooting_touch: u8,
    pub moving_footwork: u8,
    pub standing_footwork: u8,
    pub moving_jump: u8,
    pub standing_jump: u8,
    pub sprint_speed: u8,
    pub lateral_speed: u8,
    pub upper_body_strength: u8,
    pub core_strength: u8,
    pub lower_body_strength: u8,
    pub reaction_speed: u8,
    pub offensive_instincts: u8,
    pub defensive_instincts: u8,
}
